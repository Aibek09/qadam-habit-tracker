import { integer, real, sqliteTable, text, uniqueIndex, index } from "drizzle-orm/sqlite-core";
export const habits = sqliteTable("habits", { id: integer("id").primaryKey({ autoIncrement: true }), title: text("title").notNull(), emoji: text("emoji").notNull().default("✓"), target: real("target").notNull().default(1), unit: text("unit").notNull().default("раз"), kind: text("kind", { enum: ["habit", "meteor"] }).notNull().default("habit"), scheduledDate: text("scheduled_date"), sortOrder: integer("sort_order").notNull().default(0), active: integer("active", { mode: "boolean" }).notNull().default(true) }, t => [index("idx_habits_kind_active").on(t.kind, t.active)]);
export const entries = sqliteTable("entries", { id: integer("id").primaryKey({ autoIncrement: true }), habitId: integer("habit_id").notNull().references(() => habits.id, { onDelete: "cascade" }), date: text("date").notNull(), value: real("value").notNull().default(0) }, t => [uniqueIndex("idx_entries_habit_date").on(t.habitId, t.date), index("idx_entries_date").on(t.date)]);
